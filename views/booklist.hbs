<h2>τα βιβλία μου - ({{books.length}})</h2>

<style>
    table tbody{
        border-top:15px solid white;
    }
</style>

{{#if books}}
    <div class="table-responsive-sm">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Τίτλος</th>
                    <th>Συγγραφέας</th>
                    <th class="d-print-none">
                        <a class="btn btn-sm btn-success" href="/addbookform">Προσθήκη</a>
                    </th>
                </tr>
            </thead>
            <tbody>
                {{#each books}}
                    <tr>
                        <td>{{this.title}}</td>
                        <td colspan="2">{{this.author}}</td>
                        
                        <th class="d-print-none">
                            <div class="col mt-2">
                                <a class="btn btn-sm btn-danger bg-danger" href="/delete/{{this.title}}">Διαγραφή</a>
                                <a class="btn btn-sm btn-info bg-info text-white" href="/commentSection/{{this.title}}">Σχόλια</a>
                            </div>
                        </th>
                       
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
{{/if}}
{{#unless books}}
    <div class="row mt-5">
        <div class="col">
            Δεν υπάρχει κανένα βιβλίο στο myBooks
        </div>
    </div>
    <div class="row mt-2">
        <div class="col">
            <a class="btn btn-sm btn-success" href="/addbookform">Προσθήκη</a>
        </div>
    </div>
{{/unless}}